<Window x:Class="DataGrid.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>

        <DataGrid  x:Name="dataGrid" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Name}" Header="姓名" Width="80" IsReadOnly="True"   />
                <DataGridComboBoxColumn x:Name="comboBox" SelectedItemBinding="{Binding Class}" Header="班级" Width="80"/>
                <DataGridCheckBoxColumn Binding="{Binding Gender}" Header="性别" Width="80" IsReadOnly="True" />               
                <DataGridTextColumn Binding="{Binding Score}" Header="成绩" Width="80" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
