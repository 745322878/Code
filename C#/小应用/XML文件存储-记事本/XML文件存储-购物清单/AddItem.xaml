<Page
    x:Class="XML文件存储_购物清单.AddItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XML文件存储_购物清单"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid  x:Name="ContentPancel">
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="70*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        
        <TextBox Grid.Row="0" x:Name="nameTxt" VerticalAlignment="Bottom"/>
        <TextBox Grid.Row="1" x:Name="NotePad" Height="500" Opacity="0.9" TextWrapping="Wrap"  />
        <Button Grid.Row="2" x:Name="BtnSave" Width="400" Content="保存" Click="BtnSave_Click"  VerticalAlignment="Top"/>

    </Grid>
</Page>
